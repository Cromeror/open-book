/**
 * Widgets — Self-contained, configurable visual blocks.
 *
 * Widgets are higher-level than molecules: they encapsulate layout + data
 * rendering logic and are designed to be placed inside module pages
 * (specialized components) or dashboards.
 */

export { MosaicWidget } from './MosaicWidget';
export type { MosaicWidgetProps, MosaicItemConfig } from './MosaicWidget';
