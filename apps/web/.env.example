# =============================================================================
# OpenBook Web - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values
# NEVER commit .env.local to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Public Variables (exposed to browser)
# -----------------------------------------------------------------------------
# IMPORTANT: Variables with NEXT_PUBLIC_ prefix are visible in the browser
NEXT_PUBLIC_API_URL=http://localhost:3001/api
NEXT_PUBLIC_APP_NAME=OpenBook

# -----------------------------------------------------------------------------
# Server-only Variables (NOT exposed to browser)
# -----------------------------------------------------------------------------
# These are only available in Server Components and API routes
API_SECRET_KEY=your_internal_api_key

# -----------------------------------------------------------------------------
# gRPC Client Configuration (Server-only)
# -----------------------------------------------------------------------------
# Connection to gRPC API server
GRPC_HOST=localhost
GRPC_PORT=50051

# Note: Certificate paths are automatically resolved from monorepo root
# The client uses certificates from: ../../apps/api/certs/
# No need to specify certificate paths here
