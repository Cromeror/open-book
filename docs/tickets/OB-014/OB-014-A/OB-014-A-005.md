# OB-014-A-005: Crear pagina de acceso denegado

## Metadata

| Campo | Valor |
|-------|-------|
| Epic | OB-014 - Configuracion de Vistas por Rol de Usuario |
| Story | OB-014-A - Contexto de autenticacion y proteccion de rutas |
| Status | pending |
| Priority | medium |
| Created | 2026-01-05 |
| Updated | 2026-01-05 |
| Labels | task, frontend, page, ui |
| Depends on | - |
| Assigned | unassigned |

## Descripcion

Crear pagina que se muestra cuando un usuario intenta acceder a una ruta para la cual no tiene permisos.

## Contexto Tecnico

### Archivos afectados
- `apps/web/src/app/acceso-denegado/page.tsx`

### Enfoque tecnico
Pagina informativa con opcion de volver al inicio o a la seccion permitida segun rol.

## Criterios de Aceptacion

- [ ] Pagina muestra mensaje claro de acceso denegado
- [ ] Boton para volver al inicio segun rol
- [ ] Diseño consistente con la aplicacion
- [ ] Accesible desde cualquier redireccion

## Notas de Implementacion

```typescript
// app/acceso-denegado/page.tsx
'use client';

import { useAuth } from '@/hooks/useAuth';
import Link from 'next/link';

export default function AccesoDenegadoPage() {
  const { isAdmin, isResident, isAuthenticated } = useAuth();

  const getHomeLink = () => {
    if (isAdmin) return '/admin/dashboard';
    if (isResident) return '/resident/inicio';
    return '/login';
  };

  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <div className="max-w-md w-full text-center p-8">
        <div className="text-6xl mb-4">🚫</div>
        <h1 className="text-2xl font-bold text-gray-900 mb-2">
          Acceso Denegado
        </h1>
        <p className="text-gray-600 mb-6">
          No tienes permisos para acceder a esta seccion.
          {!isAuthenticated && ' Por favor inicia sesion.'}
        </p>
        <Link
          href={getHomeLink()}
          className="inline-block px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition"
        >
          {isAuthenticated ? 'Volver al inicio' : 'Iniciar sesion'}
        </Link>
      </div>
    </div>
  );
}
```

## Testing

- [ ] Pagina renderiza correctamente
- [ ] Link vuelve a la seccion correcta segun rol
- [ ] Mensaje claro y amigable
